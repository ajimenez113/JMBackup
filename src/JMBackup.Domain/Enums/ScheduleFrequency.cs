namespace JMBackup.Domain.Enums;

/// <summary>Frecuencia de un horario (RF-30).</summary>
public enum ScheduleFrequency
{
    Daily = 0,
    Weekly = 1,
    Biweekly = 2,
    Monthly = 3,
    Custom = 4,
}
